# Denetim raporu: Konfigürasyon / ortam / varsayılan / pin / yol tuzakları

- **Tarih:** 2026-08-29
- **Taban:** `feat/p0-retrieval-correctness` @ dd4f251 (int8 geçişi UYGULANMADAN önce; `config.py` hâlâ `data/index-traincompat-1bit` + eşik 60.0)
- **Ajan:** read-only denetim ajanı #2 (opus); canlı :7860 sunucusuna dokunulmadı
- **Sentez:** `docs/research/findings/2026-08-29-config-coupling-audit.md`

Ampirik olarak doğrulanan üç şey: (a) `PackedIndex.load('data/index-traincompat-int8')` → `FileNotFoundError: .../tokens.npy`; (b) `BG_TOP_K=abc belge-gozu --help` → `ValidationError`, komut hiç açılmıyor; (c) `BG_QUERY_FORMAT_ID=bogus` (ve boş string) → `ValueError: 'bogus' is not a valid QueryFormatChoice`, yine `--help` dahil tüm CLI ölü. `data/bench/results/int8-threshold-transfer.json`: int8 top-1 skorları 0.5679–0.6866 (binary'de 59.65–78.50).

---

## Bulgular

| # | Dosya:satır | Tuzak | Tetikleyici senaryo | Şiddet | En küçük düzeltme |
|---|---|---|---|---|---|
| 1 | `src/belge_gozu/app/main.py:52` | `PackedIndex.load(s.index_dir)` sabit; sınıf seçimi `manifest.quantization`'a bakmıyor | `BG_INDEX_DIR=data/index-traincompat-int8` → compat kontrolüne varmadan `FileNotFoundError: tokens.npy` (doğrulandı) | **Kritik** | `load_index(dir)` dispatcher: `read_manifest().quantization` → `PackedIndex`/`Int8Index` |
| 2 | `src/belge_gozu/index/compat.py:30-53` | `quantization` hiçbir yerde karşılaştırılmıyor (manifest.py:93'te var, yazılıyor, telemetriye basılıyor) | Manifest "int8" derken bellekte 1-bit vektörler; hiçbir kontrol itiraz etmiyor | **Kritik** | `expected_quantization` parametresi + `Settings.expected_quantization` |
| 3 | `src/belge_gozu/index/hub.py:19-21` | `upload_folder` uzaktan dosya SİLMEZ; `path_in_repo="index"` tek sabit slot, 7 yerel dizin için | 1bit push'undan sonra int8 push → uzakta `tokens.npy`+`page_vecs.npy`+`codes.npy`+`scales.npy` birlikte, tek `manifest.json` | **Kritik** | `delete_patterns=["*"]` ya da `path_in_repo=f"index/{quant}"` + pull'da eşleşme |
| 4 | `src/belge_gozu/index/hub.py:43-45` | `mkdir(exist_ok=True)` + dosya dosya `shutil.copy`, temizlik yok | int8 pull'u 1bit dizinine iner; bayat `tokens.npy` kalır; `corpus_checksum` (manifest.py:103-107) yalnız `page_ids.json`+`meta.parquet` kapsadığı için **tüm compat kontrolleri geçer** → manifest int8 der, servis bayat 1-bit skorlar | **Kritik** | Pull'u temiz geçici dizine yapıp `shutil.rmtree`+`move`; checksum'a dosya listesi ekle |
| 5 | `src/belge_gozu/cli.py:640` + `Dockerfile:10` | `if pull and s.hf_dataset_repo:` → repo boşsa `--pull` **sessizce no-op**; Dockerfile `--pull` diyor ama `BG_HF_DATASET_REPO` set etmiyor (`Dockerfile:8` yalnız `BG_DEVICE`) | `docker run` → pull atlanır → `data/` imajda yok (`Dockerfile:6` sadece `src`) → `FileNotFoundError` | **Kritik** | `--pull` verilip repo boşsa `typer.BadParameter`; Dockerfile'a `ENV BG_HF_DATASET_REPO=` + healthcheck |
| 6 | `src/belge_gozu/cli.py:198-204` + `:148-156` | `index build` yalnız `packed`/`f16` üretir; `--out` yokken hedef `s.index_dir`. CRITICAL-1 guard'ı query_format/doc_prompt bakar, **quantization'a bakmaz** | `index_dir=…-int8` iken belgelenmiş `uv run belge-gozu index build` → int8 dizinine `tokens.npy`+`sign-1bit` manifest yazar, `codes.npy`/`scales.npy` orada kalır → karışık dizin | **Kritik** | Guard'a `Precision→quantization` vs. beklenen kuantizasyon karşılaştırması ekle |
| 7 | `src/belge_gozu/config.py:55` + `src/belge_gozu/answer/base.py:47` | `min_score_threshold=60.0` binary `128−2·H` ölçeğine gömülü; `AskService` ham karşılaştırma yapar | int8'e geçince top-1 ≈ 0.57–0.69 → **her istek abstain**, LLM hiç çağrılmaz, `/healthz` yine "ok" der | **Kritik** | (bilinen) Eşiği kuantizasyona bağla ya da skoru normalize et |
| 8 | `src/belge_gozu/config.py:32` | `query_format_id: str` — Literal DEĞİL (`doc_prompt_id:36` ve `retrieval_pipeline:42` Literal) | `BG_QUERY_FORMAT_ID=bogus` **veya boş string** pydantic'ten geçer, `cli.py:77`'de ham `ValueError` → tüm CLI + `--help` ölür; `main.py:57`'de aynı hata uvicorn factory'de (doğrulandı) | **Yüksek** | `Literal["cpe-0.3.18","train-compat-v1"]` yap |
| 9 | `src/belge_gozu/cli.py:76-78` | `_CLI_DEFAULTS = Settings()` import anında; console-script `from belge_gozu.cli import app` satırında patlar | Herhangi bir bozuk `BG_*` (env veya `.env`) → `corpus/index/metrics/bench/serve` + `--help` + `--version` hepsi ham traceback (doğrulandı) | **Yüksek** | `try/except ValidationError` → `typer.echo` + `raise typer.Exit(2)`; ya da `lru_cache`'li lazy default |
| 10 | `src/belge_gozu/cli.py:280-307` (özellikle `:306`) | `index write-manifest --legacy`'nin `--out`'u yok; doğrudan `s.index_dir`'e yazar ve **üretim manifest'ini** geçersiz bir manifest'le (`mask_policy="none"`, `cpe-0.3.18`) ezer | Kullanıcı `compat.py:24`'ün "manifest yok" mesajını okuyup teşhis için çalıştırır → prod manifest'i kaybolur | **Yüksek** | `--out` zorunlu kıl; mevcut `manifest.json` varsa reddet |
| 11 | `.env` `HF_KEY` | `huggingface_hub` `HF_TOKEN`/`HUGGING_FACE_HUB_TOKEN` okur; `hub.py:9` `HfApi()` token geçmez; `Settings`'te alan yok, `config.py:11` `extra="ignore"` yutar → anahtar hiç kullanılmaz | `index push` / özel repo pull → 401, ".env'de anahtar var" yanılgısıyla | **Yüksek** | `Settings.hf_token` ekleyip `HfApi(token=...)`'a geçir; ya da `.env.example`'da `HF_TOKEN` adını kullan |
| 12 | `.github/workflows/ci.yml:11` vs `Dockerfile:5` | CI `uv sync --extra dev` (**`--frozen`/`--locked` yok**), Docker `uv export --frozen`. CI `ml` extra'sını hiç çözmez, imaj hiç build edilmez | `uv.lock` bayatlar/kayar; CI yeşil, deploy anında `uv export --frozen` patlar veya farklı sürüm gelir | **Yüksek** | CI'ya `uv sync --locked --all-extras` + bir `docker build` adımı |
| 13 | `pyproject.toml:25` | `colpali-engine>=0.3`, `torch>=2.4` gevşek; format işi 0.3.18 davranışına bağlı (`manifest.py:47-49`). `transformers` doğrudan bağımlılık **değil** ama `engine_versions`'a yazılıyor | Lock yenilenince 0.4.x gelir; `visual_prompt_prefix` değişir; `processor-default` yolunda sessizce başka doküman prompt'u | **Yüksek** | `colpali-engine>=0.3.18,<0.4`; `transformers`'ı açık bağımlılık yap |
| 14 | `src/belge_gozu/index/compat.py` (engine_versions dalı yok) | `engine_versions` `cli.py:210,294`'te yazılıyor, `manifest.py:90`'da saklanıyor, **hiç karşılaştırılmıyor** | Farklı engine sürümüyle inşa edilmiş indeks fail-fast'e takılmaz | **Orta** | `manifest.engine_versions["colpali-engine"]` vs `_engine_versions()` — major/minor uyuşmazlığında problem ekle |
| 15 | `src/belge_gozu/index/encode.py:100,105` | `from_pretrained(...)` çağrılarında `revision=` **yok** (model ve processor) | HF'de `vidore/colSmol-500M` yeni commit alır → build sessizce başka ağırlıkla | **Yüksek** | `revision=s.model_revision` ekle; `Settings.model_revision` alanı |
| 16 | `src/belge_gozu/index/encode.py:107` | `model_revision = getattr(model.config, "_commit_hash", None) or "unknown"` — private transformers alanı; `"unknown"` olunca `compat.py:33` kontrolü **tamamen atlanır** | transformers bu alanı düşürür / model yerel dizinden yüklenir → revizyon koruması sessizce ölür | **Yüksek** | `"unknown"` ise problem ekle (sessizce geçme) |
| 17 | `tests/retrieval/test_semantic_canary.py:145-158` | `xfail(strict=True)`: "OOD skorları eşiğin ÜSTÜNDE" gerçeğini kilitliyor | int8'te OOD top-1 = 0.5679–0.6866 < 60.0 → assert **geçer** → XPASS → strict xfail testi KIRMIZI yapar; "eşik kalibre oldu" gibi okunur | **Yüksek** | int8'e geçmeden xfail reason'ı ve ölçümü yenile; eşiği ölçekle birlikte taşı |
| 18 | `tests/retrieval/test_semantic_canary.py:63-68` | Skip guard yalnız `s.index_dir.exists()`; ardından `PackedIndex.load` | int8 dizini VAR → skip yok → 4 canary testi birden `FileNotFoundError` ile ERROR | **Yüksek** | Guard'a `(s.index_dir/"tokens.npy").exists()` ya da dispatcher ekle |
| 19 | `tests/conftest.py` (geneli) | BG_* ortam izolasyonu **yok**; `Settings` `.env`'i CWD'den okur (`config.py:11`) | Kabukta/`.env`'de `BG_ALLOW_INDEX_MISMATCH=true` → `tests/app/test_compat.py:152-161` (fail-fast bekliyor) kırılır; `BG_QUERY_FORMAT_ID` tüm app testlerini kırar | **Yüksek** | autouse fixture: `for k in os.environ: if k.startswith("BG_"): monkeypatch.delenv(k)` + `_env_file=None` |
| 20 | `tests/index/test_manifest.py:49-61` | Drift-kilidi yalnız `query_format_id` + `doc_prompt_id` kapsıyor | `index_dir`, `min_score_threshold`, fikstürün `quantization="sign-1bit"` (`:36`) kilitsiz → int8 geçişini hiçbir test görmez | **Yüksek** | Kilide `s.index_dir` adı/kuantizasyonu ve eşik-ölçeği assert'i ekle |
| 21 | `tests/test_config.py:4-9` | `test_defaults` `index_dir`'i ve `min_score_threshold`'u hiç assert etmiyor | Üretim indeksi/eşiği sessizce değişir, hiçbir kırmızı yok | **Orta** | İki satır assert |
| 22 | `src/belge_gozu/retrieval/core.py:42` | `self.index.page_vecs` — `page_vecs` yalnız `PackedIndex.build`'in ürettiği 1-bit artefakt (`store.py:45`); `Int8Index`'te alan hiç yok | `BG_RETRIEVAL_PIPELINE=two-stage` + int8 dizini: `main.py:52` daha önce `tokens.npy` yok diye patlar → mesaj "bu indeks two-stage'i desteklemiyor" değil, alakasız bir dosya hatası | **Yüksek** | Dispatcher + `create_app`'ta pipeline↔index-tipi çapraz kontrolü (`two-stage` yalnız `PackedIndex`) |
| 23 | `src/belge_gozu/cli.py:404` ve `:287` | `bench run` ve `index write-manifest` de `PackedIndex.load(s.index_dir)` | Üretim int8 olunca ikisi de aynı ham hatayla ölür | **Orta** | Aynı dispatcher |
| 24 | `README.md:152` | `index build … -> data/index-traincompat-1bit/ (packed, binary)` | Kullanıcı bu satırı okur, #6'yı tetikler | **Yüksek** | Satırı `index derive` zinciriyle değiştir (`build --precision f16` → `derive --quant int8`) |
| 25 | `README.md:191-198` | `BG_MIN_SCORE_THRESHOLD=60.0` + 59.85/63.40/78.50 dağılımları — hepsi binary ölçek | int8'te sayılar 0.5–0.7 bandında; belgelenen eşik anlamsız | **Yüksek** | Ölçekle birlikte yeniden ölç ve yaz |
| 26 | `README.md:79-82` | "58 MB vs 474 MB vs 919 MB" + "production index is still 1-bit … deferred to P1" | int8 üretim olunca doğrudan yanlış | **Orta** | Boyut + gerekçe cümlesini güncelle |
| 27 | `README.md:47,63` ve `:101-106` | "~1.2 s/query" (binary yol) ve skorun `128 − 2×Hamming` tanımı | int8'te 0.24 s ve float MaxSim | **Orta** | Mermaid etiketi + skor tanımını güncelle |
| 28 | `README.md:142-143` | `BG_HF_DATASET_REPO=barandincoguz/belge-gozu-index serve --pull` — 2026-08-27 yeniden inşasından sonra push kaydı yok (git log/findings/lokal `data/index`'te `manifest.json` **yok**) | Uzakta v0 duruyorsa: taze klonda `compat.py:24` fail-fast (sunucu hiç açılmaz); mevcut klonda yerel manifest hayatta kalır ama `corpus_checksum` tutmaz | **Yüksek** | Push'u doğrula/yenile; README'ye "önce `index push`" notu (uzaktaki içerik yerelde doğrulanamadı) |
| 29 | `src/belge_gozu/telemetry/prom.py:18-19` | `SCORE_BUCKETS=(45,50,55,58,60,62,65,70,75)`, `MARGIN_BUCKETS=(0,…,8)` binary ölçeğe gömülü | int8'te tüm gözlemler ilk kovaya düşer → Grafana `bg_retrieval_top_score`/margin panelleri düz çizgi | **Orta** | Kovaları eşikten türet ya da kuantizasyona göre seç |
| 30 | `src/belge_gozu/cli.py:215` | `render=RenderConfig()` — her zaman VARSAYILAN (`dpi=150`) yazar; gerçek dpi `cli.py:125`/`corpus/render.py:11,25`'ten gelir | `corpus render --dpi 72` + `index build` → manifest `dpi:150` **yalanı**; compat `render`'ı hiç karşılaştırmaz | **Orta** | dpi'yi `Settings`'e taşı, manifest'e gerçek değeri yaz, compat'a ekle |
| 31 | `tests/index/test_manifest.py:36` + `tests/app/test_api.py:196` | Fikstür ve assert `"sign-1bit"` literal'ini elle taşıyor | int8 geçişinde iki ayrı yerde elle düzeltme gerekir, unutulursa yeşil kalır | **Orta** | Fikstürü `Settings`'ten türet (mevcut kilit desenine ekle) |
| 32 | `tests/retrieval/canary_expectations.json:2-3` | `long_query_gold_rank_max=1221`, `_measured: "data/index-traincompat-1bit"` | int8 farklı temsil; cırcır yanlış tabana oturur (geçer ama anlamsızlaşır) | **Orta** | int8'te yeniden ölç, `_measured` damgasını güncelle |
| 33 | `scripts/d1_augmentation.py:21` + `:110-113` + `:170` | Docstring örneği `--index data/index-traincompat-1bit`; help "paketli (PackedIndex)"; `PackedIndex.load` | Runbook adımı int8 dizinine yönlendirilirse ham `FileNotFoundError` | **Orta** | Docstring'i güncelle; `--index` için erken `tokens.npy` kontrolü |
| 34 | `src/belge_gozu/cli.py:332` | `metrics export --out` varsayılanı sabit `Path("data/exports/events.parquet")`, oysa kaynak DB `:337`'de `s.data_dir`'den okunuyor | `BG_DATA_DIR=/başka/yer` → doğru DB'den okur, **yanlış yere** yazar | **Orta** | `s.data_dir / "exports" / "events.parquet"` |
| 35 | `src/belge_gozu/cli.py:390,421` | `bench run` `--bench` ve `out_path` sabit `data/...` literal'i | Aynı `BG_DATA_DIR` kopukluğu | **Düşük** | `s.data_dir` üzerinden çöz |
| 36 | `src/belge_gozu/config.py:14,18` | `data_dir`/`index_dir` **göreli** yollar; her şey CWD'ye bağlı | `make serve` repo kökünden çalışır; systemd/Docker/başka dizinden `FileNotFoundError` | **Orta** | `create_app`'ta `.resolve()` + var-yok kontrolü, açık hata mesajı |
| 37 | `src/belge_gozu/config.py:25-28` + `answer/gemini.py:38-47` + `answer/base.py:52-55` | API anahtarı yoksa startup'ta değil ilk `/ask`'ta patlar, `AskService` bunu "degraded"a çevirir, `/healthz` "ok" der | Anahtarsız deploy sessizce "servis kullanılamıyor" cevabı üretir | **Orta** | `create_app`'ta anahtar boşsa uyar (ya da `/healthz`'a `answerer_ready` ekle) |
| 38 | `src/belge_gozu/config.py:63` | `log_query_text: bool = True` — ham sorgu metni varsayılan olarak sqlite'a yazılıyor; Dockerfile bunu kapatmıyor | Public Space deploy'unda kullanıcı sorguları diske düşer | **Orta** | Dockerfile'da `ENV BG_LOG_QUERY_TEXT=false` |
| 39 | `src/belge_gozu/index/manifest.py:49` | `CPE_0_3_18_DOC_PROMPT` üretimde kullanılmıyor (`DOC_PROMPTS:75` `processor_default→None`); yalnız test/script'te | Kurulu engine'in ClassVar'ı değişirse sabit sessizce yalana döner; `processor-default` yolunda `main.py:81` `doc_prompt_sha256=None` → kontrol de kapanır | **Orta** | Import anında `ColIdefics3Processor.visual_prompt_prefix == CPE_0_3_18_DOC_PROMPT` doğrulaması (slow test) |
| 40 | `observability/prometheus.yml:6` | `host.docker.internal:7860` sabit; `serve --port` (`cli.py:637`) değiştirilebilir | `serve --port 8000` → target down, dashboard sessizce boş | **Düşük** | Port'u compose env değişkeni yap |
| 41 | `src/belge_gozu/bench/oracle.py:45-47` | `FloatIndex.build`'de all-zero padding reddi **yok** (`store.py:40-41`'deki muadili var) | f16 master'a padding sızarsa ancak günler sonra `index derive --quant sign-1bit`'te yakalanır | **Düşük** | Aynı `np.abs(e).sum(axis=1)==0` kontrolünü ekle |
| 42 | `src/belge_gozu/cli.py:46` | `DEFAULT_MANIFEST` ölü sabit; gerçek yol `_manifest_path` (`:101`) üzerinden `s.data_dir`'den geliyor | Birisi "varsayılan burası" diye düzeltir, hiçbir şey değişmez | **Düşük** | Sabiti sil |

---

## Verified-safe (bakıldı, tuzak değil)

- **`index build` CRITICAL-1 guard'ı** (`cli.py:148-156`) — `--out` yokken query_format/doc_prompt sapmasını gerçekten reddediyor, `tests/test_cli.py:126-136` ile kilitli. (Yalnız quantization ekseni açık → satır 6.)
- **Tek-sözlük kuralı** (`manifest.py:59-62,74-77` ← `cli.py:77-78` + `main.py:57-58`), `tests/test_cli.py:108-123` kilidi: CLI varsayılanı ile serve config'i literal kopya taşımıyor, sürüklenemez.
- **`bench oracle` çapraz kontrolleri** (`cli.py:470-526`): `_require_same_corpus` (sıra dahil), `query_format.format_id`, `doc_prompt_sha256` — üç kol için de tam; int8 kolu `codes.npy` varlığını da doğruluyor.
- **`index derive` guard'ları** (`cli.py:235-249`): `embs.npy`/`meta.parquet` varlığı, `--out == --from` reddi, `quantization=="float16"` zorunluluğu, manifest'in en sonda tek doğru yazımı (`:259-276`).
- **`scripts/verify_canary.py:610-637`** — `--index-dir` varsayılanı `Settings().index_dir`'den gelir, göreli ise `REPO_ROOT`'a çözülür, yalnız `page_ids.json` okur (`:325-326`) → **int8 dizininde de sorunsuz çalışır**.
- **`tests/retrieval/test_semantic_canary.py:15-17,48-50`** — hiçbir veri dosyası import/collection anında okunmuyor; `pytest -m "not slow"` bu dosyaya dokunmuyor.
- **`data/index-traincompat-int8/manifest.json`** — `model_revision`, `query_format`, `doc_prompt_sha256`, `corpus_checksum`, `n_pages`, `n_tokens` alanları `-f16` ile birebir aynı, yalnız `quantization` farklı → türetme zinciri temiz, dizin eksiksiz (`codes.npy`+`scales.npy`+`offsets.npy`+`page_ids.json`+`meta.parquet`+`manifest.json`).
- **`Dockerfile:5`** `uv export --frozen` — lock'a sadık (asimetri CI tarafında, satır 12).
- **`compat.py:50-52` corpus_checksum** — `page_ids.json`/`meta.parquet` değişimini gerçekten yakalıyor; `tests/test_cli.py:63` ayrıca `idx.page_ids == meta.page_id.tolist()` sıra eşitliğini kilitliyor.
- **`chunking.py:12` `CHUNK_TOKENS`** — üç kopya tek kaynağa indirilmiş, üç tüketici de `ClassVar` ile test-override edilebiliyor; kopya sürüklenmesi kapalı.
- **`main.py:374-382` `/pages`** — `images_root` altında + `.webp` + `is_file()`, `resolve()` ile symlink dahil traversal kapalı, reddedilenler 404.
- **`.gitignore:1-15`** — `data/*` + `!data/manifest/**` + `!data/bench/**`; 60 MB `tokens.npy` ve `.env` repoya girmiyor, bench sonuçları izleniyor.
- **App testlerinde `min_score_threshold=-1e9`** (`test_api.py:43` vd.) — eşik değişimi app testlerini kırmaz, yani eşik düzeltmesi bu testlerde gizli bağımlılık yaratmıyor.
- **`_load_bench_mode` R15 varsayılanı** (`cli.py:371-385`, `--all` default) — insan doğrulaması bitmeden bench'in hiç koşamaması riskini kapatıyor, `tests/test_cli.py:295-342` ile kilitli.
- **`doc_prompt_id` ve `retrieval_pipeline`** (`config.py:36,42`) — `Literal`, bozuk env'de okunabilir pydantic hatası veriyor (doğrulandı). Sorun yalnız `query_format_id`'de (satır 8).

---

## Ajanın sıralı önerisi (int8 geçişi öncesi zorunlu gördükleri)

1. `load_index(dir)` dispatcher + `compat`'a `expected_quantization` (satır 1, 2, 6, 22, 23) — bunlar olmadan geçiş ilk saniyede `FileNotFoundError`.
2. Eşiğin ölçek taşıması + `SCORE_BUCKETS` + xfail reason'ının yeniden ölçümü (satır 7, 17, 29).
3. Hub push/pull temizlik semantiği (satır 3, 4) — bu ikisi karışık dizini **compat'tan geçiren** tek yol.
4. `Dockerfile`/`serve --pull` sessiz no-op'u (satır 5) + `HF_KEY`→`HF_TOKEN` (satır 11).
5. `config.py:32` Literal'e çevir + `_CLI_DEFAULTS` hata yakalama (satır 8, 9) — beş dakikalık iş, tüm CLI'yı ham traceback'ten kurtarır.
