# Denetim raporu: Ölçek/birim bağlaşımları (skor ölçeği sınıfındaki tüm tuzaklar)

- **Tarih:** 2026-08-29
- **Taban:** `feat/p0-retrieval-correctness` @ dd4f251 (int8 geçişi UYGULANMADAN önce)
- **Ajan:** read-only denetim ajanı #1 (opus) — her satır okunmuş `dosya:satır` alıntısına dayanır
- **Görev:** `min_score_threshold`-temsil bağlaşımı (K2/K3) sınıfındaki TÜM ölçek/birim tuzaklarını çıkar
- **Sentez:** `docs/research/findings/2026-08-29-config-coupling-audit.md` (triyaj orada; bu dosya ham envanterdir)

Not: Satır numaraları dd4f251 anlık görüntüsüne aittir; int8 geçiş commit'i sonrası kaymış olabilir.

---

## Bulgular

| # | Dosya:satır | Bağ (neyin neye sessizce bağlı olduğu) | int8'de kırılır? | Şiddet | En küçük düzeltme |
|---|---|---|---|---|---|
| 1 | `src/belge_gozu/retrieval/types.py:6` | `PageHit.score` çıplak `float` — hangi temsilden geldiğini taşımıyor. Eşik, UI, Prometheus, SQLite, bench hepsi bu alanı okuyor; ölçek bilgisi hiçbir yerde yok. Sınıf-tanımlayıcı bug'ın KÖKÜ. | EVET | kritik | `PageHit`'e `score_scale: str` (ya da `quantization`) alanı; retriever doldursun |
| 2 | `src/belge_gozu/config.py:42` | `retrieval_pipeline: Literal["exhaustive","two-stage"]` — "pipeline" ekseni var, "temsil" ekseni YOK. `app/main.py:100-103` yalnız bu Literal'e dallanıyor → int8'i serve edecek hiçbir konfigürasyon mevcut değil | EVET | kritik | Ayrı `index_representation` ayarı + loader dispatch'i |
| 3 | `src/belge_gozu/app/main.py:52` | `PackedIndex.load(s.index_dir)` sabit; `index/compat.py:31-52` model/revision/query_format/doc_prompt/mask_policy/checksum'ı kontrol ediyor ama **`manifest.quantization`'ı ASLA**. Loader ile manifest çelişebilir, teşhis çıkmaz | EVET | kritik | `check_compatibility`'ye quantization kontrolü + `manifest.quantization`'a göre loader seçimi |
| 4 | `src/belge_gozu/index/hub.py:43-45` | `pull_index` hedef dizini temizlemeden dosya-dosya `shutil.copy`. int8 pull'u eski `tokens.npy`/`page_vecs.npy` üstüne düşer; `corpus_checksum` (`manifest.py:103-107`) yalnız `page_ids.json`+`meta.parquet` hash'lediği için **karma dizin tüm compat kontrollerinden geçer** ve BAYAT binary token'lar YENİ offsets'le servis edilir | EVET | kritik | Geçici dizine indir + atomik takas (ya da hedefi önce temizle) |
| 5 | `src/belge_gozu/cli.py:263` | `index derive` yazma tarafında aynı tuzak: `--out` yalnız `--from`'a karşı kontrol ediliyor (`:241-242`), boş olması istenmiyor. `Int8Index.save` (`quantize.py:102-109`) ile `PackedIndex.save` (`store.py:51-58`) ayrık dosya kümeleri yazıyor | EVET | önemli | `--out` boş değilse reddet ya da bilinen indeks dosyalarını sil |
| 6 | `src/belge_gozu/telemetry/prom.py:18` | `SCORE_BUCKETS = (45,50,55,58,60,62,65,70,75)` binary ölçeğe gömülü. int8'de her gözlem `le="45"` kovasına düşer; `bg_retrieval_top_score` tek dikene çöker | EVET | kritik | Kovaları temsile göre seç ya da skoru [0,1]'e normalize edip sabit kovalar kullan |
| 7 | `src/belge_gozu/telemetry/prom.py:19` | `MARGIN_BUCKETS = (0,0.5,1,2,4,8)` — binary margin'ler 0.2–8, int8 margin'leri ~1e-3–5e-2 → hepsi `le="0.5"` | EVET | önemli | Göreli margin `(s1−s2)/|s1|` kaydet, kovaları ona göre kur |
| 8 | `src/belge_gozu/telemetry/prom.py:53-58` | `top_score`/`margin` histogramlarının **etiketi yok**. `app/main.py:107-110` quantization'ı içeren `index_revision`'ı hesaplıyor ama bu iki seriye hiç geçmiyor → geçiş sonrası binary ve int8 gözlemleri aynı seride geri döndürülemez şekilde karışır | EVET | kritik | `quantization` etiketi ekle (kardinalite düşük) |
| 9 | `src/belge_gozu/app/main.py:163` | `margin = hits[0].score - hits[1].score` — mutlak fark, skorun yerel biriminde; tek tüketicisi ölçek-duyarsız `MARGIN_BUCKETS` | EVET | önemli | Göreli margin ya da ölçek-normalize fark |
| 10 | `src/belge_gozu/telemetry/schema.py:11` | `top_score REAL, margin_1_2 REAL` — geçişten sonra tek kolonda iki ölçek. Ayırt etme yolu yalnız `index_revision` (`app/main.py:107-110`) / `detail.retrieval.quantization` (`:214`), **ikisi de nullable** (`:113-116`) ve hiçbir test bunu kilitlemiyor | riskli | önemli | `index_revision`'ı NOT NULL yap ya da ayrı `score_scale` kolonu; test ekle |
| 11 | `src/belge_gozu/app/static/index.html:280` | `let THRESHOLD = 60.0;` — `/healthz` gecikirse/başarısız olursa binary-ölçek sabiti kullanılır (fetch `:282-287` asenkron; ilk render yarışabilir) | EVET | önemli | Fallback'i kaldır; eşik gelene kadar çubukları çizme |
| 12 | `src/belge_gozu/app/static/index.html:322` | `maxV = Math.max(...scores, THRESHOLD) * 1.08` — skorların ve eşiğin POZİTİF olduğunu varsayıyor. int8'de OOD sorgu negatif top-skor üretebilir → `maxV < 0` → tüm oranlar işaret değiştirir | EVET | önemli | `maxV = Math.max(...scores, TH, 0)` + eksen tabanını `Math.min(...scores, 0)` yap |
| 13 | `src/belge_gozu/app/static/index.html:336` + `:135-137` | `width:${(h.score/maxV*100)}%` negatifse **geçersiz CSS**; `.barfill` blok seviyesi olduğu için `width:auto`'ya düşer = **tam genişlik**. En kötü skor en dolu çubuk olarak çizilir | EVET | önemli | Genişliği `Math.max(0, …)` ile kırp |
| 14 | `src/belge_gozu/app/static/index.html:277` | `fmt(n, d=1)` — 1 ondalık. int8'de beş hit de "0,6" olarak görünür; grafiğin tek sayısal okuması sabitlenir | EVET | önemli | Ondalık sayısını skor aralığından türet (ör. `maxV<10 ? 3 : 1`) |
| 15 | `src/belge_gozu/app/static/index.html:286,337` | `eşik kontrolü (${fmt(THRESHOLD)})` ve `eşik ${fmt(THRESHOLD)}` — aynı yuvarlama; eşik "0,5"/"0,0" olur | EVET | küçük | 14 ile aynı düzeltme |
| 16 | `src/belge_gozu/app/static/index.html:351` | Grafik altyazısı "MaxSim, sorgu jetonu başına ortalama" — doğru ama temsile bağlı; int8'de "dot-product ortalaması" olur, sunucudan okunmuyor | EVET | küçük | Metni `/healthz`'ten gelen alandan üret |
| 17 | `src/belge_gozu/app/main.py:228-234` | `/healthz` yalnız `pages` + `threshold` veriyor; **quantization/temsil/`top_k`/pipeline vermiyor** → 11–16 ve 30–31'deki her UI varsayımının düzeltilebileceği tek yer kapalı | EVET | önemli | `/healthz`'e `quantization`, `index_revision`, `top_k`, `pipeline` ekle |
| 18 | `src/belge_gozu/app/main.py:141` | `threshold=s.min_score_threshold` yalnız `bg_app_info` **string etiketi** olarak gidiyor; PromQL'de `bg_retrieval_top_score` ile karşılaştırılabilir bir Gauge yok | HAYIR | küçük | `bg_score_threshold` Gauge ekle, dashboard'a eşik çizgisi |
| 19 | `observability/grafana/.../belge-gozu.json:307` | "Top skor dağılımı" heatmap'i `bg_retrieval_top_score_bucket`'a bağlı; kendi ekseni yok ama kovaları miras alıyor → 6'yla birlikte tek satıra dejenere olur | EVET | önemli | 6 düzeltilince kendiliğinden düzelir; panele eşik çizgisi ekle |
| 20 | `observability/grafana/.../belge-gozu.json:258-266` | "Abstain oranı %" stat paneli tek `green` threshold adımı, alert yok → geçişten sonraki **%100 abstain yeşil** görünür | EVET | önemli | 20/50/80 için sarı/turuncu/kırmızı adım + alert |
| 21 | `observability/grafana/.../belge-gozu.json:249` vs `src/belge_gozu/app/main.py:338-341` | Grafana paydası tüm `/ask` (error dahil), SQL tanımı `status <> 'degraded'` — aynı isimle iki farklı "abstain oranı" | HAYIR | önemli | Payda tanımını eşitle |
| 22 | `docs/research/metrics-catalog.md:34,89-90` | `top_score` birimi "skor birimi (spec eşiği ile aynı ölçek)" — tam olarak tipsiz bağın kendisi; `:89-90` binary kova sayılarını referans katalog olarak yayımlıyor | EVET | önemli | Birimi temsile göre tanımla; kova tablosuna quantization sütunu |
| 23 | `docs/research/metrics-catalog.md:31-32,66` | `stage1_ms`/`stage2_ms` kaynağı `stage1_hamming`/`stage2_maxsim` deniyor; üretim hattı `exhaustive_maxsim` yazıyor (`retrieval/core.py:123`) → o iki kolon üretimde her satırda NULL (`app/main.py:185-186`), `bg_stage_duration_seconds` etiket listesinde `exhaustive_maxsim` yok | HAYIR | önemli | Kataloğu düzelt; `exhaustive_*` için kolon ya da json_extract notu |
| 24 | `src/belge_gozu/retrieval/core.py:123` | int8 skorlama aynı `stage("exhaustive_maxsim")` adını kullanırsa `bg_stage_duration_seconds{stage="exhaustive_maxsim"}` 1.08 s binary ile 0.24 s int8'i (README:75-77) aynı seride karıştırır | riskli | önemli | Aşama adına temsili göm (`exhaustive_int8`) ya da etiket ekle |
| 25 | `src/belge_gozu/config.py:44-55` | Eşiğin gerekçe yorumu (70.6/52.4, 59.85/63.40/78.50) skorları **birimi hiç adlandırmadan** binary olarak belgeliyor; geçişten sonra aktif olarak yanıltıcı olur | EVET | önemli | Yorumda ölçeği açıkça yaz; eşiği temsil başına ayarlanabilir yap |
| 26 | `tests/retrieval/test_semantic_retrieval_eval.py:145-158,177,190` | `xfail(strict=True)` gerekçesi binary skorları gömüyor; test `top >= get_settings().min_score_threshold` karşılaştırıyor. int8'e geçince **her OOD skoru eşiğin altında** kalır → xfail beklenmedik biçimde GEÇER → `strict=True` süiti KIRMIZI yapar ve mesaj "kalibrasyon düzeldi, xfail'i kaldır" der. Gerçekte her şey abstain'e düşmüştür | EVET | kritik | Testi mutlak eşik yerine "cevaplanabilir vs. cevaplanamaz dağılım ayrımı" üzerine kur |
| 27 | `tests/index/test_manifest.py:36` + `:49-61` | Fikstür `quantization="sign-1bit"`; `test_make_manifest_defaults_track_production_config` query_format ve doc_prompt'u `Settings`'e kilitliyor ama **quantization'ı kilitlemiyor** — fikstür sürüklenmesini durduran testin deliği tam da değişecek eksende | riskli | önemli | Teste quantization assert'i ekle (Settings'e bir `index_quantization` alanı gerekir) |
| 28 | `tests/app/test_api.py:196` | `assert detail["retrieval"] == {"query_format": "train-compat-v1", "quantization": "sign-1bit"}` — literal quantization; fikstür 1-bit kaldığı için üretim int8'e geçse bile YEŞİL kalır, hiçbir şey yakalamaz | riskli | önemli | Beklentiyi `manifest.quantization`'dan oku |
| 29 | `README.md:103,123,191-200` | Kamuya açık iddialar: skor tanımı `128 − 2×Hamming`, eşik 60.0, dağılım aralıkları — hepsi binary ölçeğe bağlı ve int8 geçişinde toptan yanlış olur | EVET | önemli | Ölçeği temsile göre parametreli anlat |
| 30 | `src/belge_gozu/app/static/index.html:235,285` | "… exhaustive MaxSim → **ilk 5**" — 5 sabit, `Settings.top_k`'dan kopuk; `/healthz` top_k vermiyor | HAYIR | küçük | 17 ile birlikte `/healthz`'ten oku |
| 31 | `src/belge_gozu/app/static/index.html:300` | `pacing = [0, 900, 2100, 2600]` ms — ~1.2 s binary tarama süresine (README:63) kalibre; int8'de tarama 0.24 s (README:76-77) → şerit ~4x geriden gelir | EVET | küçük | Gerçek aşama sürelerini `/ask` gövdesine koy |
| 32 | `src/belge_gozu/app/static/index.html:311` + `:234-237` | LLM aşaması literal `j === 3` ile tanımlı, dört `<li class="stage">`e bağlı. P1'de bir aşama eklenince "atlandı" işareti sessizce yanlış satıra kayar | HAYIR | küçük | `data-st` yerine `data-role="llm"` ile seç |
| 33 | `src/belge_gozu/answer/base.py:12` ↔ `index.html:409,434` | `ABSTAIN_TEXT` iki dilde birebir kopya; `/ask` hem eşik-abstain hem degraded için `abstained=true` döndüğü (`base.py:48,55`) için UI ayrımı **yalnız bu string eşitliğiyle** yapıyor. Python'da tek harf değişirse mühür sessizce kaybolur | HAYIR | önemli | API'ye `abstain_reason: "threshold"\|"degraded"` alanı |
| 34 | `src/belge_gozu/answer/base.py:13-16` | `SERVICE_ERROR_TEXT` UI'da hiç yok → `index.html:435` servis hatasına da soluk "abstained" stili uyguluyor, `:311` "Gemini çağrılmadı — eşik altı" diyor (yalan) | HAYIR | önemli | 33 ile aynı alan |
| 35 | `src/belge_gozu/app/main.py:177` ↔ `src/belge_gozu/answer/gemini.py:12-13` | `honest_miss = "bulamadım" in answer.text.lower()` — prompt'taki "'verilen sayfalarda bulamadım' de" talimatının ikinci kopyası. Prompt sözcüğü değişirse metrik sessizce sıfırlanır. Ayrıca `.lower()` yerel-duyarsız: `"BULAMADIM".lower()` → `"bulamadim"` ≠ `"bulamadım"`; doğru fonksiyon (`tr_lower`) `scripts/verify_retrieval_eval.py:86-93`'te ZATEN var | HAYIR | önemli | Tek sabit + `tr_lower`'ı paylaşılan modüle taşı |
| 36 | `src/belge_gozu/config.py:42` ↔ `cli.py:49-51` ↔ `bench/harness.py:66` ↔ `:84` | Aynı pipeline kimliğinin dört yazımı: `"exhaustive"` (Literal), `Pipeline.exhaustive` (StrEnum), adapter `name="exhaustive"`, StageRecord `stage="exhaustive-binary"`. `events.pipeline` (`app/main.py:204`) config'inkini, bench raporu adapter'ınkini yazıyor; **hiçbiri quantization içermiyor** → `pipeline='exhaustive'` geçişten önce ve sonra iki farklı algoritma demek olur | EVET | önemli | Tek StrEnum; `pipeline` değerine temsili ekle |
| 37 | `src/belge_gozu/index/store.py:11` ↔ `retrieval/core.py:24` ↔ `:105` ↔ `index/quantize.py:60` | Modelin gömme boyutu **128** dört bağımsız literal olarak yazılı. Yalnız `binarize_pack` guard'lı; diğer üçü sessiz | riskli | önemli | `index/chunking.py`'nin yanına `EMBED_DIM = 128`, dördü de import etsin |
| 38 | `src/belge_gozu/retrieval/core.py:13-14` | `_as_u64`: `(n,16) uint8 → (n,2) uint64` — şekil guard'ı YOK; 16-byte sözleşmesi yalnız yorumda. Doğruluk tamamen `binarize_pack`'in upstream kontrolüne bağlı | riskli | küçük | `assert packed.shape[1] == EMBED_DIM // 8` |
| 39 | `src/belge_gozu/index/quantize.py:68,73` | `127.0` ve `np.clip(-127,127)` iki ayrı literal; int8 skor aralığını belirleyen sabit hiçbir tüketiciye bağlanmamış | riskli | küçük | `INT8_MAX = 127` sabiti |
| 40 | `src/belge_gozu/index/chunking.py:12` ↔ `retrieval/core.py:101` ↔ `quantize.py:93` ↔ `bench/oracle.py:85` | `CHUNK_TOKENS=500_000` üç tüketici, **iki farklı override mekanizması**: core/quantize instance `ClassVar`'ı okuyor, `native_float_scores` hiç override alamıyor (`chunk_bounds(offsets)` çağrısı parametresiz). Ayrıca `quantize.py:96`'daki `codes.astype(float32)*scales` = 500k×128×4 ≈ **256 MB tepe bellek** — sabit örtük bir bellek bütçesine bağlı | HAYIR | küçük | `native_float_scores`'a `chunk_tokens` parametresi; bellek bütçesini yoruma yaz |
| 41 | `src/belge_gozu/corpus/render.py:22` ↔ `scripts/verify_retrieval_eval.py:282` ↔ `app/main.py:374` | `images/<doc>/<page:04d>.webp` sözleşmesi üç yerde ayrı ayrı yazılı (üretici, doğrulayıcı, güvenlik allowlist'i) | HAYIR | önemli | Tek `page_image_path(doc_id, page_no)` yardımcısı |
| 42 | `src/belge_gozu/bench/dataset.py:79` ↔ `scripts/verify_retrieval_eval.py:131,281,343` | `"doc:page"` ayrıştırması dört yerde; tek üretici `corpus/render.py:29` | HAYIR | önemli | `parse_page_id()` / `make_page_id()` yardımcıları |
| 43 | `src/belge_gozu/cli.py:215` | `render=RenderConfig()` — manifest **her zaman varsayılan** dpi/quality'yi yazıyor; `corpus render --dpi 300` ile üretilen indeks manifest'inde yine 150 görünür. Ayrıca `RenderConfig.quality=80` (`manifest.py:83`) `render.py:26`'daki 80'in ikinci kopyası | HAYIR | önemli | Gerçek render ayarlarını `meta.parquet` yanında sakla ve manifest'e taşı |
| 44 | `src/belge_gozu/cli.py:177` ↔ `index/encode.py:127` | `batch_size = 1` ("optimize edip büyütmeyin", MPS determinizmi) yalnız ÇAĞIRAN'da; encoder'ın kendi `range(0, len(images), 4)` batch döngüsü duruyor → başka her çağıran için determinizm garantisi sessizce kaybolur | HAYIR | önemli | Garantiyi `encode_pages` içine taşı (ya da orada da 1 yap) |
| 45 | `src/belge_gozu/bench/harness.py:68,180` ↔ `tests/retrieval/retrieval_regression_expectations.json:2` | `record_top=200`; gold sırası yalnız ilk 200'de aranıyor, yoksa `-1`. Cırcır dosyası aynı gold'u **1221** olarak kaydediyor → P1'in var olma sebebi olan sorgu harness'ta `-1` görünür | HAYIR | önemli | `rank_of` ile tam-korpus sırasını kaydet |
| 46 | `src/belge_gozu/bench/harness.py:166` vs `src/belge_gozu/cli.py:535` | `bench run` ks=`(1,5,10,20,50,200)`, `bench oracle` ks=`(1,5,20,50,200)` — iki farklı recall@k kataloğu aynı isimle yayımlanıyor | HAYIR | küçük | Tek `BENCH_KS` sabiti |
| 47 | `src/belge_gozu/bench/harness.py:189` | `final_ranked = ranked[: max(ks)]` — kesme `record_top`'a değil `ks`'e bağlı; iki sabit sessizce 200'de buluşuyor | HAYIR | küçük | `record_top` kullan |
| 48 | `tests/retrieval/retrieval_regression_expectations.json:2-3` | `long_query_gold_rank_max: 1221`, `_measured` alanı yalnız düzyazı ("data/index-traincompat-1bit"). Hangi indekste ölçüldüğü makine tarafından hiç doğrulanmıyor; int8'e geçilince cırcır sessizce başka bir temsile uygulanır | riskli | önemli | Beklentiyi `quantization` anahtarına göre sakla; test yüklü manifest'le eşleştirsin |
| 49 | `src/belge_gozu/cli.py:404` | `bench run` de `PackedIndex.load(s.index_dir)` sabit — geçişten sonra `serve`'ün servis ettiği indeksi benchmark edemez | EVET | önemli | Loader dispatch'ini paylaş |
| 50 | `src/belge_gozu/cli.py:528-529,548,598-604` | `bench oracle` binary kolu `ExhaustiveBinaryRetriever`, int8 kolu `Int8Index.score_all` — **farklı ölçekli skorlar aynı rapora** giriyor. Bugün yalnız sıra karşılaştırıldığı için zararsız; kol başına eşik uygulanır uygulanmaz kırılır. Rapor `summary`'sinde ölçek notu yok | riskli | küçük | Rapora kol başına `score_scale` yaz |
| 51 | `src/belge_gozu/app/main.py:242` + `:36-38` | `k = body.k or s.top_k` — `SearchBody.k` sınırsız/doğrulanmamış: `k=0` sessizce 5 olur, `k=-1` tuhaf dilim, `k=100000` tüm korpusu tek yanıtta döndürür | HAYIR | önemli | `Field(ge=1, le=50)` |
| 52 | `src/belge_gozu/app/main.py:41-42` | `AskBody.question: str` uzunluk sınırı yok; `answer/gemini.py:72` istek başına 5 tam görüntüyü belleğe alıyor. Hiçbir yerde istek boyutu limiti yok | HAYIR | önemli | `Field(max_length=...)` + gövde boyutu limiti |
| 53 | `src/belge_gozu/config.py:24` vs `:59-60` | `gemini_model="gemini-3.6-flash"` ile `gemini_price_in/out = 0.10/0.40` arasında hiçbir bağ yok; `app/main.py:172-174` koşulsuz çarpıyor. `prom.py:65` `bg_llm_cost_usd` Counter'ının **model etiketi yok** → model/fiyat değişince maliyet aynı seride birikmeye devam eder | HAYIR | önemli | Fiyatları `{model: (in,out)}` sözlüğüne bağla; Counter'a `model` etiketi |
| 54 | `src/belge_gozu/app/main.py:168-169` | `tps = tokens_out / (answer_ms/1000)`; `answer_ms` (`answer/base.py:50`) görüntü yükleme + ağ turunu da kapsıyor → `bg_llm_tokens_per_second` (kovalar 5…160) üretim hızı değil gidiş-dönüş ölçüyor | HAYIR | küçük | Metrik adını/dokümantasyonu düzelt |
| 55 | `src/belge_gozu/app/main.py:334` vs `src/belge_gozu/cli.py:359` | `/stats` p95 son **10 000** satırdan, `metrics summary` p95 **tüm** satırlardan. `docs/research/metrics-catalog.md:7-14` ikisini "birebir aynı formül" diye sunuyor ama pencere farkını hiç anmıyor | HAYIR | önemli | Pencereyi eşitle ya da katalogda belirt |
| 56 | `src/belge_gozu/cli.py:637` ↔ `observability/prometheus.yml:6` ↔ `scripts/loadgen.py:86` | 7860 portu üç bağımsız literal | HAYIR | küçük | Tek yerden (env/`Settings`) türet |
| 57 | `scripts/loadgen.py:52` / `corpus/manifest.py:101` / `corpus/download.py:54` / `telemetry/recorder.py:60` | Dört bağımsız timeout sabiti (120 / 20 / 60 / 5000 ms), hiçbiri diğerinden türetilmemiş; sunucu tarafında hiç timeout yok | HAYIR | küçük | `Settings`'e timeout alanları |
| 58 | `scripts/ab_st_reference.py:59` | `REFERENCE_DOC_TOKENS = {"train-compat": 871, "cpe-0.3.18": 875}` — dpi=150/quality=80 ile render edilmiş TEK sayfaya bağlı ölçüm (`corpus/render.py:25-26`, `cli.py:125`). Render ayarı değişirse referans kontrolü "şablon farklı" diyerek yanlış teşhis verir | HAYIR | küçük | Sayıları render ayarıyla birlikte anıp uyarı bas |
| 59 | `scripts/ab_st_reference.py:55` vs `tests/index/test_encode_mask.py:94` | Aynı "sign uyuşması" sözleşmesi için iki bağımsız eşik: `AGREEMENT_TARGET = 0.99` ve testte kayıtlı ölçüm 0.9990/0.9989 | HAYIR | küçük | Tek sabit |
| 60 | `src/belge_gozu/app/static/index.html:212` | Manşet `4.222 sayfa · 50 kanun · 1928–2024` — sayfa sayısı `/healthz`'ten güncelleniyor (`:284`), **50 ve tarih aralığı hiçbir zaman** güncellenmiyor; `data/state.json`'da 56 belge var | HAYIR | küçük | Sayıları `/healthz`'ten türet ya da iddiayı kaldır |

---

## Doğrulanmış güvenli (tuzak DEĞİL)

- **`index_revision` quantization taşıyor** — `app/main.py:107-110` `corpus_checksum[:12]/query_format/quantization` üretiyor ve `events.index_revision` + `detail.retrieval.quantization` (`:214`) olarak yazılıyor. Yani SQLite tarafında geçmiş satırlar **ayırt edilebilir** (Prometheus tarafı değil — bkz. #8/#10).
- **`binarize_pack` boyut guard'ı** — `store.py:11-12` `(n,128)` değilse `ValueError`. int8 dizini binary yola verilirse gürültülü patlar, sessiz kalmaz.
- **`PackedIndex.build` sıfır-token / padding guard'ları** — `store.py:37-41`; `reduceat`'in boş segment görmemesi buna dayanıyor ve yorumda açıkça belgelenmiş (`core.py:107-108`).
- **`ExhaustiveBinaryRetriever` / `Int8Index` / `native_float_scores` normalizasyonu** — üçü de `out / max(1, n_q)` yapıyor (`core.py:110`, `quantize.py:100`, `oracle.py:94`). Per-query-token ortalama sözleşmesi üç uygulamada tutarlı; kırılan yalnız aralık, formül değil.
- **Atıf indeksleri `[Sn]`** — `gemini.py:25` `f"[S{i+1}]"`, `:79` regex, `:83` `0 < i <= len(pages)`. Sınırlar `len(pages)`'ten geliyor; "tam 5 hit" varsayımı YOK.
- **`n_suffix=10`** — `manifest.py:24,39`'daki iki kopya birincil kaynağa karşı doğrulanmış veri (`ab_st_reference.py:89-99` input_ids birebir kontrolü); sürüklenen bir sabit değil.
- **`CHUNK_TOKENS` birleştirmesi** — `index/chunking.py` gerçekten üç kopyayı birleştirmiş; `chunk_bounds` geç bağlama (`:23-24`) ve `bounds` testleri (`test_exhaustive.py:43-48`, `test_quantize.py:69-84`) chunk sınırının skoru etkilemediğini kilitliyor.
- **`bench run` doküman prompt'u geçmiyor** (`cli.py:407`) — ilk bakışta uyumsuzluk gibi duruyor ama `visual_prompt_prefix` yalnız `process_images`'ı etkiliyor (`encode.py:54-62,128`) ve bench yalnız sorgu encode ediyor. Zararsız.
- **`slice`/`query_style` literal'leri** — `bench/dataset.py:8-22` tek kaynak, pydantic yükleme anında doğruluyor (`load_bench:100-102`); `tests/test_cli.py:271,279`'daki literal'ler test verisi, ikinci sözleşme değil.
- **`QUERY_FORMATS`/`DOC_PROMPTS` tek sözlük** — `manifest.py:59-77`; CLI (`cli.py:77-78`), serve (`app/main.py:57-58`) ve slow retrieval_eval fikstürü (`test_semantic_retrieval_eval.py:70-71`) hepsi buradan okuyor. Bu eksen gerçekten birleştirilmiş.
- **`corpus_checksum` kapsamı** — `manifest.py:103-107` yalnız `page_ids.json` + `meta.parquet` hash'liyor; **embedding dosyalarını kapsamıyor**. Tek başına bug değil ama #4'teki karma-dizin tuzağının neden sessiz kaldığının açıklaması.
- **`_STAGE_COLS` dışı aşamalar düşmüyor** — `prom.py:111-114` `detail["stages"]`'ten toplayıp histograma yazıyor, `test_prom.py:92-97` kilitliyor. Yeni bir int8 aşama adı Prometheus'ta görünür (ama etiketsiz — bkz. #24).
