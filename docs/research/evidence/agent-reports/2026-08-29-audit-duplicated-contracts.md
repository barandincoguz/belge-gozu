# Denetim raporu: Mükerrer sabitler / örtük sözleşmeler envanteri

- **Tarih:** 2026-08-29
- **Taban:** `feat/p0-retrieval-correctness` @ dd4f251 (int8 geçişi UYGULANMADAN önce)
- **Ajan:** read-only denetim ajanı #3 (opus) — `src/`, `tests/`, `scripts/`, `observability/`, `index.html`, `metrics-catalog.md`, `README.md` tamamı okundu; doküman iddiaları `data/index-*/` altındaki 6 gerçek manifest'e karşı çapraz kontrol edildi
- **Sentez:** `docs/research/findings/2026-08-29-config-coupling-audit.md`

---

## Duplike sözleşme envanteri

| # | Kopyalar (dosya:satır) | Sözleşme | Sapma riski / bilinen sapma | Şiddet | Tek-kaynak önerisi |
|---|---|---|---|---|---|
| 1 | `answer/base.py:12` · `app/static/index.html:409` (karşılaştırma `:434`) | `ABSTAIN_TEXT` metni birebir eşitlikle karşılaştırılır | `hardAbstain = a.abstained && a.text === ABSTAIN_TEXT` → "DAYANAK BULUNAMADI" mührü (`:245`, `:437-438`) **yalnız** string birebir tutarsa basılır. Python'daki metne tek nokta eklense mühür sessizce kaybolur; hiçbir test bu iki kopyayı karşılaştırmıyor (`test_api.py:126-131` sadece `id="q"` arıyor) | **KRİTİK** | `/healthz`'e `abstain_text` (ya da `Answer`'a `reason: "threshold"\|"degraded"`) ekle; UI metin değil **alan** karşılaştırsın. Ara adım: `test_api.py`'de `Path(STATIC_DIR/"index.html").read_text()` içinde `ABSTAIN_TEXT` arayan drift-lock |
| 2 | `answer/base.py:13-16` (UI kopyası **YOK**) | `SERVICE_ERROR_TEXT` | Asimetri: degraded yanıt `abstained=true` döner, UI `.abstained` sınıfını verir ama mühür basmaz — kota hatası ile eşik-altı abstain kullanıcıya **aynı** görünür. `status="degraded"` sunucuda var (`main.py:293-298`), `/ask` gövdesinde yok | ORTA | `/ask` yanıtına `status` alanını ekle (zaten `RequestEvent.status` olarak hesaplanıyor), UI ondan dallansın |
| 3 | `app/main.py:177` (`"bulamadım" in text.lower()`) · `answer/gemini.py:13` (SYSTEM: `'verilen sayfalarda bulamadım' de`) · `telemetry/prom.py:60` (metrik açıklaması) · `metrics-catalog.md:37` | LLM'in "dürüst ıska" ifadesi ile sayacın aradığı alt-dize aynı olmalı | Prompt'taki ifadeyi "bulunamadı"/"tespit edemedim" yapan bir düzenleme `honest_miss`'i **sessizce sonsuza dek 0** yapar; `bg_honest_miss_total` ve v0'ın "13/17" bulgusu ölçülemez hale gelir. `.lower()` üstelik Türkçe-duyarsız (`verify_canary.py:86-93`'te doğru `tr_lower` var, burada kullanılmıyor) | **YÜKSEK** | `answer/base.py`'de `HONEST_MISS_MARKER` sabiti; SYSTEM prompt onu f-string ile gömsün, `main.py` `tr_lower` ile arasın. Prompt↔marker eşitliğini kilitleyen test |
| 4 | Üretici: `retrieval/core.py:41,46,56,121,123` · `answer/base.py:50`. Tüketici: `app/main.py:166,184-186` · `telemetry/prom.py:22-27` (`_STAGE_COLS`) · `metrics-catalog.md:30-33,66` | Telemetri aşama adları: `query_encode`, `stage1_hamming`, `stage2_maxsim`, `exhaustive_maxsim`, `answerer` | `stage("...")` serbest string; yeniden adlandırma **hiçbir yerde hata vermez** — `col.stages.get("query_encode")` sessizce `None` döner, kolon NULL'a düşer, Grafana paneli boşalır. `exhaustive_maxsim` yalnız `detail.stages` fallback'iyle (`prom.py:111-114`) kurtuluyor | **YÜKSEK** | `telemetry/stages.py`'de `StageName(StrEnum)`; `stage()` imzası onu alsın, `_STAGE_COLS` anahtarları enum olsun. `set(StageName) ⊇ produced` testi |
| 5 | `bench/harness.py:17` (yorum), `:84` `"exhaustive-binary"`, `:132` `"stage1"`, `:151` `"stage2"` · `tests/bench/test_harness.py:17` `stage="final"`, `:84`, `:104` | Bench `StageRecord.stage` sözlüğü — telemetriden **tamamen ayrı** ikinci bir aşama vokabüleri | `"final"` yorumda ve testte var, üretimde **hiç üretilmiyor**. `"stage1"/"stage2"` (bench) ile `stage1_hamming`/`stage2_maxsim` (telemetri) aynı şeyi iki adla anıyor; `data/bench/results/*.json` tüketicileri iki sözlükten hangisini bekleyeceğini bilmiyor | ORTA | Aynı `StageName` enum'ını bench'te de kullan ya da `StageRecord.stage`'i `Literal[...]` yap; `"final"`'ı ya üret ya yorumdan sil |
| 6 | `config.py:42` (`Literal["exhaustive","two-stage"]`) · `cli.py:49-51` (`Pipeline` StrEnum) · `bench/harness.py:66,105` (`name = ...`) · `app/main.py:100,239,277` (çıplak `== "exhaustive"` / `== "two-stage"`) · `tests/retrieval/test_semantic_canary.py:78` · `index.html:235,265,285` (prose) | Pipeline kimliği; `RequestEvent.pipeline`'a yazılıp (`main.py:204`) rapor `config`'ine giriyor (`cli.py:429`) | Vokabülerin **6 ayrı kopyası**. `query_format`/`doc_prompt` için `QUERY_FORMATS`/`DOC_PROMPTS` ile birleştirme yapılmış, pipeline'da yapılmamış. `main.py:100`'de `else` dalı → yeni bir üçüncü pipeline adı sessizce `TwoStageRetriever`'a düşer (fail-fast yok) | **YÜKSEK** | `Pipeline` StrEnum'ı `retrieval/`'a taşı; `Settings.retrieval_pipeline: Pipeline`, `main.py` `match` + `case _: raise`. `harness` adapter `.name` alanları enum'dan okusun |
| 7 | `cli.py:59-61` (`Quantization`: yalnız `sign-1bit`, `int8`) · `cli.py:200` (`quantization = "float16"` **çıplak literal**, enum'da yok) · `cli.py:203,297` · `index/quantize.py:25,77` · `bench/oracle.py:22` (docstring) · `index/compat.py` — **quantization kontrolü hiç yok** | `IndexManifest.quantization: str` sözlüğü: `"sign-1bit"` \| `"int8"` \| `"float16"` | `Quantization` enum eksik üyeli: `float16` üç yerde çıplak string. `compat.check_compatibility` `quantization`'ı **hiç kontrol etmiyor** → `BG_INDEX_DIR=data/index-traincompat-int8` verildiğinde `PackedIndex.load` `tokens.npy` yok diye patlıyor (uyum hatası olarak değil; `2026-08-29-e2e-review.md:165`). R16 int8 entegrasyonu geldiğinde bu vokabüler dört yerden birden değişecek | **YÜKSEK** | `Quantization`'ı `index/manifest.py`'a taşı + `float16` üyesi ekle; `IndexManifest.quantization: Quantization`; `check_compatibility`'ye `expected_quantization` kontrolü |
| 8 | `config.py:32` (`query_format_id: str` — **çıplak str**) vs `config.py:36,42` (`doc_prompt_id`/`retrieval_pipeline`: `Literal`) · dönüşüm `main.py:57` ve `cli.py:77` | Config alanlarının tip disiplini | Asimetri: `BG_QUERY_FORMAT_ID=saçma` pydantic doğrulamasından **geçer**, sonra `cli.py:77 QueryFormatChoice(...)` **import anında** `ValueError` atar → `belge-gozu --help` dahil tüm CLI çöker. Diğer iki alan aynı hatayı temiz bir `ValidationError` ile verir | ORTA | `query_format_id: QueryFormatChoice`; `cli.py:77-78` dönüşümü gereksizleşir |
| 9 | `bench/dataset.py:79` · `scripts/verify_canary.py:131,281,343` · `bench/dataset.py:34` (docstring) · `docs/research/evidence/canary-schema.md` (`"dok:sayfa"`) · `corpus/render.py:29` (üretici: `f"{doc_id}:{i}"`) | `page_id == "<doc_id>:<page_no>"`, sayfa 1-tabanlı | `split(":", 1)[0]` deseninin 4 kopyası; üretici (`render.py:29`) ile tüketiciler arasında paylaşılan fonksiyon yok. `doc_id` içinde `:` geçse hepsi farklı davranırdı; 1-tabanlılık yalnız `verify_canary.py:337` yorumunda yazılı | ORTA | `corpus/page_id.py`: `make_page_id(doc_id, page_no)` / `split_page_id(pid) -> (str, int)`; render + dataset validator + verify_canary onu çağırsın |
| 10 | `corpus/render.py:22` (`f"images/{doc_id}/{i:04d}.webp"`) · `scripts/verify_canary.py:282` · `tests/conftest.py:28` · `tests/retrieval/test_core.py:44` · `tests/index/test_encode_mask.py:107` · `scripts/ab_st_reference.py:58` · `app/main.py:374-380` (`.webp` + `images/` kökü doğrulaması) | Görüntü yolu şeması `images/<doc_id>/<page:04d>.webp` | `:04d` genişliği **iki bağımsız yerde** elle yazılı. 10000+ sayfalı bir belge, ya da `:05d`'ye geçiş, `verify_canary --review`'ın görüntü açmasını sessizce bozar (dosya yok → sadece uyarı). `/pages`'in `.webp` kısıtı üçüncü kopya | ORTA | Aynı `corpus/page_id.py` modülünde `image_rel_path(doc_id, page_no)`; render, verify_canary, fikstürler oradan |
| 11 | `telemetry/schema.py:3-23` (`EVENTS_DDL` kolon listesi) · `telemetry/schema.py:34-61` (`RequestEvent` alanları) · `telemetry/recorder.py:11-40` (`_COLUMNS`) · `telemetry/recorder.py:65` (migrasyon listesi) · `metrics-catalog.md:22-50` | Olay şeması: 28 alan, üç elle-tutulan paralel liste | Bugün üçü **senkron** (elle doğrulandı) ama kilit yok: `test_schema.py:52-63` yalnız 10 adlık bir **alt küme** kontrol ediyor. `RequestEvent`'e alan eklenip `_COLUMNS`'a eklenmezse sqlite named-param bağlaması fazla anahtarı **sessizce yok sayar** → veri kaybı, üstelik `record()` hatayı yutuyor (`recorder.py:84-87`) | **YÜKSEK** | `_COLUMNS = [f for f in RequestEvent.model_fields]`; DDL'i aynı listeden üret ya da `set(_COLUMNS) == set(RequestEvent.model_fields) == PRAGMA table_info` eşitliğini kilitleyen tek test |
| 12 | `docs/research/metrics-catalog.md` ↔ `telemetry/prom.py`, `telemetry/schema.py`, `app/main.py` | Metrik/kolon kataloğu | **BİLİNEN, ÖLÇÜLMÜŞ SAPMA — 8 kalem:** (a) `pipeline`/`index_revision` kolonları katalogda **yok** (`schema.py:59-60`); (b) `:66` `bg_stage_duration_seconds` etiket kümesi `{query_encode, stage1_hamming, stage2_maxsim, answerer}` — `exhaustive_maxsim` **eksik**, yani üretimin tek gerçek retrieval aşaması katalogda yok; (c) `:75` `bg_app_info` etiketleri `index_revision`/`query_format` **eksik** (`prom.py:83-92`); (d) `:31-32` `stage1_ms`/`stage2_ms` "REAL" (NULL değil) ve kaynağı `retrieval/core.py` diyor — varsayılan `exhaustive`'de **daima NULL** (README:169-172 doğru anlatıyor, katalog anlatmıyor); (e) `:39` `candidates` aynı şekilde daima NULL; (f) `:50` `detail` içeriği eksik — gerçek `detail` ayrıca `stages` ve `retrieval:{query_format,quantization}` taşıyor (`main.py:213-214`); (g) satır referansları bayat: `:37` `app/main.py:118` → gerçek `:177`, `:48` `app/main.py:113-115` → gerçek `:171-174`; (h) `:29` `total_ms` kaynağı "`RequestTimer`" — kod tabanında böyle bir sınıf **yok** (0 eşleşme) | **YÜKSEK** | Katalog tablosunu `RequestEvent.model_fields` + `PromMetrics.__init__` üzerinden üreten bir `scripts/gen_metrics_catalog.py` + CI'da `--check`. Satır numaralarını referanslardan tümüyle çıkar |
| 13 | `observability/grafana/.../belge-gozu.json` (9 panel, 12 seri adı) ↔ `telemetry/prom.py:36-68` | PromQL sorgularındaki seri adları | Dashboard `bg_*_total`/`_bucket` adlarını elden yazıyor; `prom.py`'deki `Counter("bg_abstain", ...)` adının değişmesi paneli **sessizce boşaltır** (Grafana "No data" der, hata vermez). `bg_retrieval_score_margin`, `bg_honest_miss_total`, `bg_index_pages`, `bg_app_info` tanımlı+belgeli ama **hiçbir panelde kullanılmıyor** | ORTA | JSON'daki her `bg_*` adını `PromMetrics().render()` çıktısında arayan tek bir test (model/ağ gerektirmez) |
| 14 | `app/main.py:334-337` · `cli.py:359,366` · `scripts/loadgen.py:24-27` · iddia: `metrics-catalog.md:7-14`, `observability-architecture.md:161-162` ("iki farklı p95 tek kod tabanında yaşayamaz"), `runbook.md:23-28` ("doğrudan karşılaştırılabilir") | nearest-rank p95 = `sıralı[min(n-1, ceil(p·n)-1)]` | Formül üç yerde birebir, **ama girdi kümesi değil**: `/stats` `ORDER BY id DESC LIMIT 10000` (`main.py:336`), `metrics summary` **tüm satırlar** (`cli.py:359`). 10k olaydan sonra iki p95 farklılaşır → belgelenmiş "doğrudan karşılaştırılabilir" iddiası **şimdiden yanlış**. loadgen'de ayrıca fazladan `max(0,...)` sargısı var | **YÜKSEK** | `telemetry/stats.py: nearest_rank(values, p)` + `recent_total_ms(db, limit)`; üç çağıran onu kullansın, `LIMIT`'i tek yerde karara bağla ve dokümanı düzelt |
| 15 | `app/main.py:338-341` · `cli.py:351-354` (birebir aynı SQL string) · `metrics-catalog.md:101-107` | `abstain_rate = AVG(abstained) WHERE endpoint='/ask' AND status <> 'degraded'` | `'degraded'` literali ve endpoint filtresi iki yerde kopya; `status` vokabülerinin (`answered\|abstained\|degraded\|error\|ok`) tek kaynağı yok. Yeni bir `status` değeri eklenirse iki sorgudan birinin güncellenmesi unutulur | ORTA | `telemetry/queries.py`'de `ABSTAIN_RATE_SQL` sabiti + `Status(StrEnum)` |
| 16 | `config.py:55` (`60.0`) · `index.html:280` (`let THRESHOLD = 60.0`) · `prom.py:18` (`SCORE_BUCKETS`) · `README.md:123,191,198` · `tests/telemetry/test_prom.py:47` | Cevap eşiği | UI kopyası `/healthz` yanıtıyla üzerine yazılıyor (`index.html:283`) — **ama** fetch başarısız olursa `60.0` ile grafik çizer, kırmızı çizgi yanlış yerde durur. `SCORE_BUCKETS` eşiğin **etrafında** kurulu (58/60/62) ama eşikten türetilmiyor: `BG_MIN_SCORE_THRESHOLD` değişirse histogram çözünürlüğü yanlış bantta kalır | ORTA | UI: fetch başarısızsa grafiği çizme (eşik bilinmiyor). `SCORE_BUCKETS`'ı eşikten türet ya da yorumla bağla |
| 17 | `config.py:38` (`top_k = 5`) · `index.html:235,285` (`→ ilk 5`, hardcoded) · `README.md:47,51,185,187` (`top-5`) · `bench/harness.py:195,202` (`recall_at_k(..., 5)`, `ndcg5`) | Sunulan sayfa sayısı | UI `h.pages`'i sunucudan alıyor ama **"ilk 5"i almıyor** — `BG_TOP_K=10` ile UI hâlâ "ilk 5" yazar. `gemini.py:25`'in `[S{i+1}]` listesi `len(pages)` kadar uzar, `MetricBlock.ndcg5` adı 5'e çakılı | ORTA | `/healthz`'e `top_k` ekle, UI ondan yazsın; README'nin "top-5"lerini `top_k` diye genelleştir |
| 18 | `index.html:212` (`4.222`), `:235` (`4.222 sayfada`) · `retrieval/core.py:77` (docstring) · `index/quantize.py:54` · `README.md:9,16,47,63,76` · `tests/telemetry/test_prom.py:42,70` | Korpus sayfa sayısı | UI'ın ikisi de JS ile `h.pages`'ten üzerine yazılıyor (`:284-285`) → JS/fetch hatasında bayat sayı görünür. README'de **5 kopya**, hiçbiri manifest'ten türetilmiyor. Doğrulandı: 6 manifest de `n_pages=4222`, yani bugün doğru | ORTA | Statik HTML'deki sayıları `—` yap (JS dolduruyor zaten); README sayısını manifest'ten üreten bir kontrol/rozet |
| 19 | `README.md:215` (`3,776,882 tokens`) ↔ `data/index-traincompat-1bit/manifest.json` (`n_tokens = 3,759,994`) · `README.md:74-80` (0.233/0.302, 1.08s/0.24s/0.08s, 58/474/919 MB) · `README.md:63` (`~1.2 s`) ↔ `core.py:77` (`~1.2 s`) ↔ `README.md:76` (`1.08 s`) | Yayına giden ölçüm iddiaları | **BİLİNEN SAPMA:** README:211-216 "yeniden inşa edilen indeks ... 3,776,882 token" diyor; bu **cpe-0.3.18** indeksinin sayısı. Bugünkü üretim indeksi (`data/index-traincompat-1bit`) **3,759,994** token. README'nin padding-fix anlatısı üretim indeksini tarif etmiyor. Ayrıca aynı gecikme iki farklı sayıyla anılıyor (`~1.2 s` M4 Pro vs `1.08 s` CPU idle) ve `core.py:77` docstring'i üçüncü kopya. Recall/boyut rakamlarının hiçbirinde kod-doküman kilidi yok | **YÜKSEK** | `n_pages`/`n_tokens`/`quantization`/`query_format`'ı manifest'ten okuyup README bloğunu doğrulayan bir `-m slow` test ya da `make docs-check`. Gecikme sayısını tek yerde tut |
| 20 | `index.html:212` (`50 kanun · 1928–2024`), `:235,265,285` (`exhaustive MaxSim`, `binary MaxSim`, `Gemini Flash yanıtlayıcı`), `:253` (`en yakın 5 sayfa`) | UI'ın anlattığı sistem künyesi | **Hiçbiri sunucudan gelmiyor** (e2e-review K16). `BG_RETRIEVAL_PIPELINE=two-stage` ile UI hâlâ "exhaustive" der; `BG_GEMINI_MODEL` değişse "Gemini Flash" kalır. `1928–2024` ile `README.md:11`'in `1928–1975`'i ayrıca çelişiyor | ORTA | `/healthz`'i (ya da yeni `/about`) `pipeline`, `gemini_model`, `top_k`, `index_revision` ile genişlet — bu değerler `prom.set_app_info` için zaten `main.py:135-144`'te hesaplanıyor |
| 21 | `tests/bench/test_dataset.py:15-38` · `tests/test_verify_canary.py:27-50` | `BenchQuestion` fikstür fabrikası (17 alanlık payload) | İki **birebir** kopya; yalnız `question_id`/`verified_by`/`verification_status` farklı. `verification_kind` gibi varsayılanlı yeni bir alan iki kopyanın birinde sessizce yanlış kalabilir | ORTA | `q_dict`'i `tests/conftest.py`'ye (ya da `tests/bench/_fixtures.py`) taşı |
| 22 | `tests/retrieval/test_semantic_canary.py:53-80` ↔ `app/main.py:52-103` | Üretim retriever'ının kurulumu (index yükle → meta oku → format çöz → encoder → pipeline dallanması) | Fixture docstring'i sapmayı kendisi itiraf ediyor ("`create_app`'ın ... mantığını birebir yansıtır"). `create_app`'a eklenen her adım burada **tekrarlanmazsa** canary testi üretimden başka bir pipeline'ı ölçer ve yeşil kalır — üstelik `-m slow` olduğu için CI hiç koşmaz | **YÜKSEK** | `create_app`'tan `build_retriever(settings, encoder=None) -> Retriever` fonksiyonunu ayır; hem `create_app` hem canary fixture onu çağırsın |
| 23 | `bench/harness.py:166` `ks=(1,5,10,20,50,200)` · `cli.py:535` `ks=(1,5,20,50,200)` · `scripts/d1_augmentation.py:49` `RECALL_KS=(1,5,10,20,50)` | Rapor edilen Recall@k kümesi | Üç rapor üreticisi **üç farklı k kümesi** kullanıyor: çıktılar k=10'da/k=200'de kıyaslanamıyor. `README.md:74` "Recall@1/5/20/50/200 all identical" yalnız oracle kümesini kapsıyor | ORTA | `bench/metrics.py`'de `DEFAULT_KS` sabiti; üç çağıran onu varsayılan alsın |
| 24 | `config.py:37` (`stage1_candidates=200`) · `retrieval/core.py:53` (`candidates: int = 200`) · `bench/harness.py:68,107,113` (`record_top=200`, `candidates=200`) · `cli.py:414` (`max(200, s.stage1_candidates)`) · `tests/answer/test_base.py:20,36,47,55,70` | Aday havuzu genişliği 200 | 200 beş yerde çıplak varsayılan. `BG_STAGE1_CANDIDATES` değiştirilirse `TwoStageRetriever.search`'ün imza varsayılanı ve bench `record_top` eski değerde kalır — ablasyon raporu config'ten sapar. `README.md:64,208` de bağımsız kopya | ORTA | İmza varsayılanlarını kaldır ya da `DEFAULT_CANDIDATES` sabiti tanımla, `Settings` onu varsayılan alsın |
| 25 | `cli.py:214` (`mask_policy="drop-padding"` yazılır) · `index/compat.py:48` (`!= "drop-padding"` kontrol) · `cli.py:298` (`"none"` legacy) · `tests/index/test_manifest.py:37` · `tests/app/test_compat.py:125` | `mask_policy` vokabüleri | Yazan ve kontrol eden iki çıplak literal; üçüncü değer `"none"` yalnız legacy komutta. `compat.py:22-29`'daki hata metni bu bağımlılığı **prose olarak** anlatıyor — literal değişirse mesaj yalan olur | ORTA | `MaskPolicy(StrEnum)` `index/manifest.py`'da; `IndexManifest.mask_policy: MaskPolicy` |
| 26 | `cli.py:215` (`render=RenderConfig()` — **daima varsayılan**) · `index/manifest.py:80-84` (`dpi=150, quality=80`) · `corpus/render.py:11` (`dpi: int = 150`), `:26` (`quality=80`) · `cli.py:125` (`--dpi` varsayılan `150`) | Render provenance'ı manifest'e yazılır | **Sessiz yalan:** `corpus render --dpi 300` ile üretilen görüntülerden inşa edilen indeksin manifest'i yine `dpi=150, quality=80` der. Render parametreleri `meta.parquet`'e de yazılmıyor, yani gerçek değer hiçbir yerde kayıtlı değil. `150` üç, `80` iki kopya | ORTA | `render_all` kullanılan dpi/quality'yi `meta.parquet` metadata'sına (ya da `data/render.json`'a) yazsın; `index build` manifest'i oradan doldursun |
| 27 | `bench/dataset.py:45` (`verification_status`) + `:56` (`verification_kind`) · `scripts/verify_canary.py:224-244` (`apply_decision`) · `:286-317` (`compute_status`) · `cli.py:371-385` (`_load_bench_mode`) · `data/bench/canary_v1.README.md:91-99` · `docs/research/evidence/canary-schema.md` (17 alan listeler, `verification_kind` **yok**) | Doğrulama iki eksenli: *durum* × *tür* | **BİLİNEN SAPMA, dokümante ama kilitlenmemiş:** (a) `apply_decision` `verification_kind`'a **hiç dokunmuyor** → bir insan `--review` ile onaylasa satır `model-cross-check` etiketli kalır, `compute_status`'ın `human_total`'ı insan emeğini **saymaz**; (b) `load_bench(only_verified=True)` `kind`'ı yok sayar → bugün 48/48 satır `verified`, yani `--only-verified` ile `--all` **aynı sonucu** veriyor ve `cli.py:376-377`'deki gerekçe bayat; (c) canary-schema.md `verification_kind`/`verification_note`'u hiç anmıyor | **YÜKSEK** | `apply_decision`'a `kind="human"` yazdır (`canary_v1.README.md:96-99`'un kendi önerisi); `load_bench`'e `require_human: bool` ekle; schema dokümanını `BenchQuestion.model_fields`'tan üret |
| 28 | `index/chunking.py:12` (çağrı anında okunur) · `retrieval/core.py:86` (`ClassVar`, **import anında** bağlanır) · `index/quantize.py:47` (aynı) · `bench/oracle.py:85` (global'i canlı okur) | Chunk boyutu tek kaynak | Birleştirme **yarım**: `oracle.native_float_scores` global'i çağrı anında okurken `core`/`quantize` import anındaki snapshot'ı taşıyor. `monkeypatch.setattr(chunking, "CHUNK_TOKENS", ...)` oracle'ı etkiler, diğer ikisini etkilemez — üç yol üç farklı semantikte | DÜŞÜK | `ClassVar` snapshot'larını kaldır; üçü de `chunk_bounds(offsets, self.chunk_tokens)` deseninde instance alanı kullansın |
| 29 | `retrieval/core.py:24` (`128 - 2*hamming`), `:105` (aynı) · `index/store.py:11-12` (`shape[1] != 128`) · `index/quantize.py:39,52,60` · `index/encode.py:18` · `bench/oracle.py:21` · `README.md:37` | Embedding boyutu 128 = binary skor tavanı | `128 - 2*ham` skorun **ölçeğini** belirliyor (eşik 60.0 buna bağlı). Model D=256'ya geçse `binarize_pack` gürültülü patlar (iyi) ama `sim = 128 - 2*ham` iki yerde elle düzeltilmeli; unutulursa skorlar sessizce negatife kayar. Yedi çıplak `128` | ORTA | `EMB_DIM = 128` sabiti; `sim = EMB_DIM - 2*ham`, `binarize_pack` de onu kullansın |
| 30 | `app/main.py:230-234` (`/healthz`) ↔ `index.html:282-286` · `main.py:311` (`/ask`) ↔ `index.html:431-457` · `retrieval/types.py:4-10` ↔ `index.html:332-393` | HTTP yanıt şeması ↔ UI alan adları | UI 11 alan adını elden okuyor; `test_api.py:60` yalnız `{page_id, score, image_path}` alt kümesini, `:52` `/healthz`'i tam kilitliyor. `PageHit` alan adı değişirse UI `undefined` render eder, test yeşil kalır | ORTA | `/ask` ve `/search` yanıtlarını da `test_api.py`'de tam anahtar kümesiyle kilitle |
| 31 | `answer/gemini.py:12` (SYSTEM: `[S1] gibi işaretle`) · `:25` (`f"[S{i+1}] ..."`) · `:79` (`re.findall(r"\[S(\d+)\]")`) · `tests/answer/test_gemini.py:23,29,65,70` · `index.html:436` (`mdLite` marker'ları **strip etmiyor**) | `[Sn]` atıf işareti sözdizimi | Üç kopya aynı dosyada (görece güvenli) ama prompt/parser bağı test edilmiyor: `test_prompt_mentions_sources` `[S1]`'i prompt'ta arar, `test_citations_parsed_from_response` `[S2]`'yi elle uydurur — biri değişse diğeri fark etmez. Kaynak sayısı **5'e bağlı değil** (`len(pages)` kadar üretilir) — ama UI ham `[S1]` metnini kullanıcıya gösteriyor, bu da yazılı olmayan bir sözleşme | DÜŞÜK | `CITATION_RE` + `SRC_MARKER` tek yerde; SYSTEM prompt f-string ile örneği ondan alsın |
| 32 | `cli.py:637` (`port=7860`) · `Dockerfile:9,10` · `observability/prometheus.yml:6` · `scripts/loadgen.py:86` · `README.md:144` · `observability-architecture.md:17,38` · `runbook.md:4` — ve `3001`: `docker-compose.yml:12` · `README.md:167` · `runbook.md:5,9,14` · `obs-arch:34,82,95` | Port numaraları | 7860 yedi, 3001 altı kopya. Port değişirse Prometheus scrape'i sessizce ölür (target down, uygulama etkilenmez) | DÜŞÜK | Prometheus target'ını `${BG_PORT:-7860}` ile env'den al; README/runbook tek bir "Portlar" tablosuna referans versin |
| 33 | `config.py:18` · `cli.py:72` (yorum) · `README.md:152` · `d1_augmentation.py:21` (docstring) · `tests/retrieval/test_semantic_canary.py:106,124,148` · `tests/retrieval/canary_expectations.json:3` | Üretim indeks dizini | Yedi kopya; altısı yorum/docstring. Indeks yolu değişince hepsi bayatlar — özellikle `canary_expectations.json:3`'ün `_measured` künyesi cırcır eşiğinin **hangi indekste** ölçüldüğünü iddia ediyor ve o iddia doğrulanmıyor | DÜŞÜK | Docstring'lerde yolu `Settings().index_dir` diye an; `canary_expectations.json`'a temsil alanı ekleyip testte manifest'e karşı doğrula |
| 34 | `config.py:19` · `scripts/ab_st_reference.py:50` · `scripts/d1_augmentation.py:48` · `tests/index/test_manifest.py:31` · `tests/app/test_compat.py:60,73,85,98,112,128,144` (7×) · `tests/test_config.py:6` · `tests/index/test_encode_mask.py:112` | `"vidore/colSmol-500M"` | 13 kopya. `test_config.py:6` `Settings`'i kilitliyor, `test_manifest.py:31` fikstürü — ama `test_compat.py`'nin 7 kopyası hiçbir şeye bağlı değil | DÜŞÜK | `test_compat.py`'de `MODEL = make_manifest().model_name`; scriptlerde `Settings().retriever_model` |
| 35 | `tests/app/test_compat.py:14-15` (`PROD_FORMAT_ID`/`OTHER_FORMAT_ID` çıplak literal) | Format kimliği vokabüleri (4. kopya) | `make_manifest` ↔ `Settings` kilidi var, bu literaller **onun dışında**. Üretim formatı değişirse bu iki sabit yanlışa döner (ama testler gürültülü kırılır) | DÜŞÜK | `PROD_FORMAT_ID = Settings().query_format_id` |
| 36 | `scripts/queries_sample.txt:1-3` · `index.html:226-228` (chip'ler) · `README.md:122-124` · `tests/retrieval/test_semantic_canary.py:40-41` (`Q_SHORT`/`Q_LONG`) · `scripts/ab_st_reference.py:51-54` (`QUERIES`) | Örnek/kanarya sorguları | Aynı üç Türkçe soru dört-beş yerde. `Q_LONG` hem canary cırcırının hem README'nin hem UI chip'inin konusu; biri düzenlenirse ötekiler ölçtüğünü ölçmez hale gelir | DÜŞÜK | Tek bir `data/bench/sample_queries.json`; UI chip'leri + testler oradan |
| 37 | `docs/research/evidence/canary-schema.md:3` ("Zorunlu 17 alan") ↔ `bench/dataset.py:29-56` (17 zorunlu + `verification_note`, `verification_kind`) | Canary satır şeması | Doküman 17 alanda donmuş; `verification_note` ve `verification_kind` sonradan eklendi ve şema belgesine yazılmadı. Yeni ajan turu bu belgeye bakarak `kind` yazmayacak → satırlar varsayılan `"human"` alacak (yanlış künye) | DÜŞÜK | Şema tablosunu `BenchQuestion.model_fields`'tan üret; ya da `verification_kind`'ı zorunlu yap |

---

## Verified-safe (tek kaynak + kilit VAR)

| Sözleşme | Tek kaynak | Kilit |
|---|---|---|
| `query_format_id` / `doc_prompt_id` sözlüğü | `index/manifest.py:52-77` (`QueryFormatChoice`+`QUERY_FORMATS`, `DocPromptChoice`+`DOC_PROMPTS`) — CLI, serve, canary fixture aynı dict'ten | `tests/test_cli.py:108-123`, `tests/index/test_manifest.py:49-61` |
| `index build`'ın üretim indeksini ezmesi | `cli.py:76-78` + `cli.py:148-156` (sapma varsa `--out` zorunlu) | `tests/test_cli.py:126-136`, `:97-105` |
| `TRAIN_COMPAT_V1.render()` ve iki doc prompt dizisi | `index/manifest.py:34-49` | `tests/index/test_manifest.py:64-80` (birebir literal doğrulaması) |
| `git_commit()` | `provenance.py:17` (`bench.harness:12` yalnız re-export) | Yön kısıtı docstring'te; üretim→bench bağımlılığı kaldırılmış |
| `USER_AGENT` | `corpus/manifest.py:13` | Yorumla belgeli, tek tanım |
| Türkçe normalizasyon (`tr_lower`/`normalize_ws`) | `scripts/verify_canary.py:86-103` — karşılaştırmanın iki tarafı da aynı fonksiyondan | `tests/test_verify_canary.py` |
| `exhaustive_maxsim`'in Prometheus'a düşmesi | `prom.py:111-114` (`detail.stages` fallback) | `tests/telemetry/test_prom.py:92-97`, `:100-110`; `tests/app/test_api.py:165-177` |
| `pipeline` / `index_revision` kolonları | `schema.py:59-60` + `recorder.py:65` migrasyonu | `tests/app/test_api.py:180-197`, `tests/telemetry/test_recorder.py:96-100`, `test_schema.py:66-78` |
| `SERVICE_ERROR_TEXT` (sunucu tarafı) | `answer/base.py:13` — testler **import ediyor** | `tests/app/test_api.py:6,236` |
| `PageHit` | `retrieval/types.py:4-10` — tek pydantic modeli | (UI tarafı için bkz. #30) |
| `check_compatibility` dalları | `index/compat.py` | `tests/app/test_compat.py` 13 test — tüm dallar kapsanmış (**`quantization` hariç** — bkz. #7) |
| `chunk_bounds` algoritması | `index/chunking.py:15-32` | Sabit okuma semantiği hâlâ üç farklı (bkz. #28), ama algoritma tek |

---

## Ajanın "en kısa yoldan kapatılacak üçlü"sü

1. **#1 (`ABSTAIN_TEXT`)** — tek satırlık drift-lock testi bugün yazılabilir; sızıntı riski en yüksek olan.
2. **#19 (README `3,776,882` ↔ üretim `3,759,994`)** — zaten yanlış; düzeltmesi bir sayı, ama kilidi olmadan tekrar sapar.
3. **#12 + #11 (metrics-catalog ↔ şema/prom)** — katalog 8 kalemde sapmış; üreten bir script tek hamlede hepsini kapatır ve #4/#13'ün de zeminini hazırlar.
