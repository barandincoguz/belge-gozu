# Domain docs

This is a single-context repository. Before engineering work, read the root
`CONTEXT.md` when present and relevant ADRs under `docs/adr/`. Neither currently
exists, so continue using the terminology in `AGENTS.md`, `docs/research/`, and
the applicable approved `docs/superpowers/` plan/spec.
