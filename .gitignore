.venv/
__pycache__/
data/*
!data/manifest/
!data/manifest/**
!data/bench/
!data/bench/**
# Korpus künyesi (56 belge x sha256+durum, 6 KB): cevaplanamaz set doğrulayıcısı
# ve testleri "çapa kanunu korpusta YOK" iddiasını BUNA karşı sınar. İzlenmezse
# taze bir klonda ne `scripts/validate_unans.py` ne de testleri koşabilir
# (2026-08-31'de CI tam bunu yakaladı).
!data/state.json
*.egg-info/
.pytest_cache/
requests.sqlite
.env
.env.*
!.env.example
.ruff_cache/
.DS_Store
.claude/

# Archify visual-check kanıtı: ~3 MB ekran görüntüsü, tek komutla yeniden
# üretilebilir (`archify visual-check <figür>.html`). Depoya girmesi gereksiz.
docs/diagrams/evidence/

# Serena önbelleği: 5,6 MB dil sunucusu indeksi, açılışta yeniden kuruluyor.
.serena/

# Dense artefakt üretim çıktısı: model başına 69 MB vektör matrisi + yarım
# kalmış checkpoint. Kalıcı kopya `belge-gozu-semantic-artifacts` Dataset'inde
# değişmez commit'le durur; depoya girmesi köken kaydını zayıflatır.
data/bench/dense-artifacts/

# Kökeni belirsiz, yarım inmiş safetensors başlığı (100 KB).
st.bin
